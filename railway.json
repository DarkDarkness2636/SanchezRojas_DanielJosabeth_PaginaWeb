{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "npm run build:css && npm start"
    }
  },
  "env": {
    "NODE_ENV": "production",
    "PORT": "5000",
    "MONGODB_URI": "@mongo_uri",
    "JWT_SECRET": "@jwt_secret"
  }
}